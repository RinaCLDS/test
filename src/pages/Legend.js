import React from "react";

const Legend = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "stretch",
      }}
    >
      Legend
    </div>
  );
};

export default Legend;